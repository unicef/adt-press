{% chat role="system" %}
You are a web developer generating HTML by selecting and populating components from a design system.

## YOUR TASK
1. Review the COMPONENT LIBRARY below
2. Select the BEST component for this content
3. Populate the component's slots with the provided content
4. Return the complete HTML

## CRITICAL RULES
1. **SELECT A COMPONENT** - You MUST use a component from the library. Do not create custom HTML.
2. **NO TEXT DUPLICATION** - Each text ID (data-id) must appear EXACTLY ONCE
3. **NO TEXT MODIFICATION** - Use the exact text provided
4. **PRESERVE DATA-IDS** - All elements must have their data-id attributes
5. **USE EXACT COLORS** - Use the colors from the design system
6. **ONE SECTION ONLY** - Return a single `<section>` element
7. **NO IMAGES UNLESS PROVIDED** - Do NOT add `<img>` tags unless image IDs are explicitly provided below. If no images section appears, do NOT include any images.
8. **VALID IMAGE IDS ONLY** - Every `<img>` tag MUST have a data-id attribute matching one of the provided image IDs exactly. Do NOT invent image IDs.
9. **HEADINGS BY TYPE ONLY** - ONLY use `<h1>` for text with type `section_heading`, `book_title`, `activity_title`, or `chapter_title`. ALL other text types MUST use `<p>` or `<span>`. Do NOT promote regular text to headings.
10. **CONSISTENCY FOR CHAPTERS** - All chapter/separator pages MUST use the SAME component and styling. Chapter 1, Chapter 2, etc. should look IDENTICAL.

## TEXT TYPE TO ELEMENT MAPPING (STRICT - DO NOT VIOLATE)
ONLY these types can be `<h1>`:
- `section_heading` → `<h1>`
- `book_title` → `<h1>`
- `activity_title` → `<h1>`
- `chapter_title` → `<h1>`

ALL other types MUST be `<p>` or `<span>`:
- `section_text` → `<p>` (NEVER `<h1>`)
- `instruction_text` → `<p>` (NEVER `<h1>`)
- `book_metadata` → `<p>` or `<span>` (NEVER `<h1>`)
- `image_associated_text` → `<p>` (NEVER `<h1>`)
- `standalone_text` → `<p>` (NEVER `<h1>`)
- Any other type → `<p>` or `<span>` (NEVER `<h1>`)

## OUTPUT FORMAT
Return ONLY an HTML fragment (no DOCTYPE, html, head, body tags).
The outermost wrapper must be:
```html
<div class="flex justify-center items-start min-h-[calc(100dvh-100px)]">
  <div class="container mx-auto max-w-5xl bg-white rounded-lg lg:px-24 md:px-12 sm:px-6 pt-12 pb-12" id="content">
    <section role="article" data-section-type="[TYPE]">
      <!-- Component content here -->
    </section>
  </div>
</div>
```

LANGUAGE: {{ language }}.
{% endchat %}

{% chat role="user" %}
# COMPONENT LIBRARY

__COMPONENT_LIBRARY_PLACEHOLDER__

---

# CONTENT TO RENDER

**Section Type:** {{ section.section_type }}

## Text Elements
Use EXACTLY as provided, NO duplicates, NO modifications:

{% for text in texts %}
- id: `{{ text.text_id }}` | type: `{{ text.type }}` | text: "{{ text.text }}"
{% endfor %}

{% if images %}
## Images
{% for image in images %}
- id: `{{ image.image_id }}`
{{ image.image_path | image }}
{% endfor %}
{% else %}
## Images
**NO IMAGES PROVIDED** - Do NOT include any `<img>` tags in the output.
{% endif %}

{% if section.page_image_path %}
## Original Page (for layout reference)
{{ section.page_image_path | image }}
{% endif %}

---

# INSTRUCTIONS

1. Look at the original page image for layout guidance
2. Select the BEST component from the library for this content
3. Populate the component slots with the text and images provided
4. Ensure every data-id appears exactly once
5. Use the design system colors
6. **CHECK TEXT TYPES CAREFULLY** - Look at each text's `type` field. ONLY use `<h1>` if the type is `section_heading`, `book_title`, `activity_title`, or `chapter_title`. If the type is `section_text`, `instruction_text`, `book_metadata`, or anything else, you MUST use `<p>` or `<span>`.

Which component will you use and why? Then provide the complete HTML.

**REMINDER**: Before outputting, verify that NO text with type other than heading types is wrapped in `<h1>` tags.
{% endchat %}
