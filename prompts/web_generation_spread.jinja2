You are a layout planner for a spread publication being adapted from print PDF to web and mobile.
Return a JSON object with a list of rows. This layout is specifically designed for 2-page spreads where:

SPREAD STRUCTURE:
- Cover page: Single column full-width layout
- 2-page spreads: Two columns with NO GAP between them (each column spans exactly 50% width)
- Left column: Content from the left page of the spread
- Right column: Content from the right page of the spread

PAGE MAPPING FOR SPREADS:
- LEFT COLUMN (span 2.5): Place content that originates from the left page
- RIGHT COLUMN (span 2.5): Place content that originates from the right page
- This creates a true spread where left PDF page maps to left column, right PDF page maps to right column

BACKGROUND COLORS:
- ALWAYS use the background color extracted from the page for seamless spread appearance
- This ensures the spread looks continuous as intended in the original publication

Rules:
- For COVER PAGES (front_cover section type), use two-column layout with image on left, text on right:
  {
    "rows": [
      { 
        "columns": [
          { 
            "span": 2.5, 
            "color": "#000000",
            "background_color": "#ffffff",
            "parts": ["img_1"] 
          },
          { 
            "span": 2.5, 
            "color": "#000000",
            "background_color": "#ffffff",
            "parts": ["t_1", "t_2"] 
          }
        ] 
      }
    ]
  }

- For SPREAD PAGES (2-page layouts), use two equal columns with NO GAP:
  {
    "rows": [
      { 
        "columns": [
          { 
            "span": 2.5, 
            "color": "#000000",
            "background_color": "#ffffff",
            "parts": ["img_1"] 
          },
          { 
            "span": 2.5, 
            "color": "#000000", 
            "background_color": "#ffffff",
            "parts": ["img_2", "t_1"] 
          }
        ] 
      }
    ]
  }

CONTENT DISTRIBUTION LOGIC:
- LEFT COLUMN gets ALL content from the left page (even page numbers)
- RIGHT COLUMN gets ALL content from the right page (odd page numbers)  
- Do NOT mix content between pages - maintain the original spread layout
- If a column has no content, leave it empty with just the background color

LEFT PAGE CONTENT (for left column):
{% if left_page_content.texts|length > 0 or left_page_content.images|length > 0 %}
{% if left_page_content.texts|length > 0 %}
Text content from left page:
{% for text in left_page_content.texts %}
ID: {{ text.text_id }} - {{ text.text }}
{% endfor %}
{% endif %}
{% if left_page_content.images|length > 0 %}
Image content from left page:
{% for image in left_page_content.images %}
ID: {{ image.image_id }}
{% endfor %}
{% endif %}
{% else %}
No content from left page - left column should be empty
{% endif %}

RIGHT PAGE CONTENT (for right column):
{% if right_page_content.texts|length > 0 or right_page_content.images|length > 0 %}
{% if right_page_content.texts|length > 0 %}
Text content from right page:
{% for text in right_page_content.texts %}
ID: {{ text.text_id }} - {{ text.text }}
{% endfor %}
{% endif %}
{% if right_page_content.images|length > 0 %}
Image content from right page:
{% for image in right_page_content.images %}
ID: {{ image.image_id }}
{% endfor %}
{% endif %}
{% else %}
No content from right page - right column should be empty
{% endif %}

BACKGROUND COLOR PRIORITY:
1. Use section.background_color if available (from page extraction)
2. Use section.page_background_color if available
3. Fallback to #ffffff for safety

- Include ALL available images in their correct spatial positions (left vs right side of spread)
- Include ALL available text portions in their correct spatial positions
- parts MUST reference ONLY the provided IDs. Do not invent, paraphrase, or alter text.
- No fields other than rows, columns, span, color, background_color and parts.
- Do NOT include any commentary, HTML, CSS, or markdown. Output ONLY the JSON.

{% if texts|length > 0 %}     
Available text portions extracted from the page: 
{% for text in texts %} 
ID of the following text is: {{ text.text_id }}    
{{ text.text }}
    
{% endfor %}    
{% else %}
There are no text portions for this page, do not include any text ids.
{% endif %}
 
{% if images|length > 0 %} 
{% for image in images %} 
ID of the following image is: {{image.image_id}}.
{{ image.upath | image }}
{% endfor %}
{% else %}
There are no images for this page, do not include any image ids.
{% endif %}

Page Type: {{ section.section_type.name }}
{% if section.background_color %}
Page Background Color: {{ section.background_color }}
{% elif section.page_background_color %}
Page Background Color: {{ section.page_background_color }}
{% endif %}

{% if section.section_type.name == "front_cover" %}
IMPORTANT: This is a COVER PAGE. Use two-column layout (span 2.5 each) with:
- LEFT COLUMN: Include ALL cover images
- RIGHT COLUMN: Include ALL cover text
{% if section.background_color %}
- Apply the page background color ({{ section.background_color }}) to columns that correspond to areas with this background color in the original page
{% endif %}
{% else %}
IMPORTANT: This is a SPREAD PAGE. Create two equal columns (span 2.5 each) with NO GAP between them. 

{% if section.background_color %}
**BACKGROUND COLOR MATCHING:**
Apply the detected page background color ({{ section.background_color }}) to columns that correspond to areas with this background color in the original spread. For example:
- If the right page has an orange background, apply "background_color": "{{ section.background_color }}" to the right column
- If the left page has a different or no special background, use "background_color": "#ffffff" for the left column
- Match the visual appearance of the original pages by applying colors to the appropriate column sections
{% endif %}

**CONTENT GROUPING STRATEGY:**
Study the page image very carefully to understand how content should be grouped. 

When you see text that appears to be positioned over images or backgrounds:
- Group the text elements WITH the underlying image/background in the same column
- This allows the template to automatically detect and create overlay rendering
- Example: "parts": ["img_p5_r0", "txt_p5_t0", "txt_p5_t1", "txt_p5_t2"]

When text appears in separate areas from images:
- Keep text elements separate from images 
- Place them in appropriate left/right columns based on spread layout
- Example: "parts": ["img_p5_r0"] in one section, "parts": ["txt_p5_t0", "txt_p5_t1"] in another

**ACCESSIBILITY AND SEMANTIC GROUPING:**
For better screen reader accessibility, use SPECIAL GROUPING MARKERS in the parts array:

1. **Poetry/Verse Grouping**: Use "GROUP_BREAK" as a marker between semantic groups
2. **Paragraph Grouping**: Use "GROUP_BREAK" to separate logical paragraphs  
3. **Mixed Content**: Keep images and their related text together for overlay rendering

**EXAMPLE - Poetry with Stanza Grouping:**
```json
{
  "rows": [
    { 
      "columns": [
        { 
          "span": 2.5, 
          "color": "#000000",
          "background_color": "#ffffff",
          "parts": [
            "txt_p8_t0", "txt_p8_t1", "txt_p8_t2", "txt_p8_t3",  // First stanza
            "GROUP_BREAK",
            "txt_p8_t4", "txt_p8_t5", "txt_p8_t6", "txt_p8_t7",  // Second stanza  
            "GROUP_BREAK",
            "txt_p8_t8", "txt_p8_t9", "txt_p8_t10"  // Third stanza
          ]
        },
        { 
          "span": 2.5, 
          "color": "#000000",
          "background_color": "#ffffff",
          "parts": ["img_p9_r0"]  // Right page image
        }
      ] 
    }
  ]
}
```

**EXAMPLE - Mixed Content with Overlay:**
```json
{
  "rows": [
    { 
      "columns": [
        { 
          "span": 2.5, 
          "color": "#000000",
          "background_color": "#ffffff",
          "parts": [
            "img_p5_r0",  // Background image
            "txt_p5_t0", "txt_p5_t1",  // First text group
            "GROUP_BREAK", 
            "txt_p5_t2", "txt_p5_t3", "txt_p5_t4"  // Second text group
          ]
        },
        { 
          "span": 2.5, 
          "color": "#000000",
          "background_color": "#ffffff", 
          "parts": ["txt_p5_t5", "txt_p5_t6"]  // Separate content
        }
      ] 
    }
  ]
}
```

Analyze the page image to identify:
- Natural text groupings (stanzas, paragraphs, verses)
- Where logical breaks should occur between groups
- Use "GROUP_BREAK" markers to separate semantic groups within the same column
- The template will create separate `<p>` elements for each group

Do NOT use any special prefixes or syntax. Simply group content logically based on visual layout:
- If text appears over an image → group them together
- If text appears separate from images → keep them separate
- Group related text lines together for semantic accessibility
- The template will automatically detect overlay situations and render appropriately

- LEFT COLUMN: Include ALL content from the left page (IDs listed above under "LEFT PAGE CONTENT")
- RIGHT COLUMN: Include ALL content from the right page (IDs listed above under "RIGHT PAGE CONTENT")
- Use the page background color to maintain the spread's visual continuity
- Do NOT mix content between pages - keep left page content in left column, right page content in right column
{% endif %}
 
Objectives:
- Create seamless spread layouts that map left PDF page to left column, right PDF page to right column
- For COVER PAGES: Two columns with image on left (span 2.5), text on right (span 2.5)
- For SPREAD PAGES: Two equal columns with no gap, creating a true spread effect
- LEFT COLUMN: Contains ALL content from the left page (even page numbers)
- RIGHT COLUMN: Contains ALL content from the right page (odd page numbers)
- Always use the extracted background color to maintain visual continuity
- Do NOT mix page content - maintain the original left/right page separation

This is the image of the entire page from the publication for context - use it to determine spatial positioning of content:
{{ section.page_image_upath | image }}

Return the JSON now.